function Allatok() {
    return(
        <div id="allatok">
            {allaok.map((allat, ix) => (
                <Kartya key={ix} allat={allat} />
            ))}
        </div>
    )
};


function Allat{
    
}